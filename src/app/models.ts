export interface Info {
  infoUrl: string;
}

export interface AppEvent {
  nom: string;
  foto: string;
  description: string;
}

export interface MemoryForm {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
}
